#include<iostream>
using namespace std;

int main(){
    int n,a,b,curr=0,cap=0;
    cin>>n;
    while(n--){
        cin>>a>>b;
        curr=curr -a+b;
        cout<<curr;
        if(cap<curr)
            cap=curr;
    }
    cout<<cap<<endl;
    return 0;
}
